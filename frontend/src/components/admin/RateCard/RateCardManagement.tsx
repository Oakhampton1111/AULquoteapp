import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

export const RateCardManagement: React.FC = () => {
  return (
    <Card>
      <Title level={2}>Rate Card Management</Title>
      <p>This is a placeholder for the Rate Card Management component.</p>
    </Card>
  );
};

export default RateCardManagement;
