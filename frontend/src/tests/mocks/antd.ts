/**
 * Mock implementations for Ant Design components
 */

// Mock the message component from Ant Design
export const mockMessage = {
  success: jest.fn(),
  error: jest.fn(),
  info: jest.fn(),
  warning: jest.fn(),
  loading: jest.fn(),
};

// Mock for Ant Design Modal component
export const mockModal = {
  confirm: jest.fn(),
  info: jest.fn(),
  success: jest.fn(),
  error: jest.fn(),
  warning: jest.fn(),
};

// Add more mock implementations as needed
